// import { createSelector } from '@reduxjs/toolkit';
// import { selectAuth } from './slices/authSlice';
// import { selectProject, ProjectState } from './slices/projectSlice';
// import { RootState } from './store';

export default function selectors() {}

// // export const project: ProjectState = createSelector(
// //   (project) => state.project,
// //   (projectName) => project.projectName,
// //   (id) => project.id,
// //   (tasks) => project.tasks
// // );

// export const project = createSelector(selectProject, (state) => state);

// console.log(selectAuth);
// console.log(selectProject);
// export default function selectors() {}

// // from project/index
// // const projectName = useSelector((state: RootState) => state.project.projectName);
// // const taskList = useSelector((state: RootState) => state.project.tasks);

// // const selectSelf = (state: State) => state;
// // const unsafeSelector = createSelector(selectSelf, (state) => state.value);
// // const draftSafeSelector = createDraftSafeSelector(selectSelf, (state) => state.value);
